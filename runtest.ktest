#!/bin/bash

require-lib tests/test-libs.sh

config-mem 256M

main()
{
    if sleep 300 ; then
	exit 1
	# this sleep must be killed via an ssh into the vm,
	# if not the test has failed
    else
	echo TEST SUCCESS
    fi
}
