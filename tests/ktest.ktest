#!/bin/bash

require-lib test-libs.sh

config-mem 256M
config-timeout 300

main()
{
    if sleep 300 ; then
	exit 1
	# this sleep must be killed via an ssh into the vm,
	# if not the test has failed
    fi
}
